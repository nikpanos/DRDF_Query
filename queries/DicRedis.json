{
  type = "redis"
  idToUri {
    hosts = [
      { address = "localhost", port = 6377 }
      { address = "localhost", port = 6378 }
      { address = "localhost", port = 6379 }
    ]
  }
  uriToId {
    hosts = [
      { address = "localhost", port = 6380 }
      { address = "localhost", port = 6381 }
      { address = "localhost", port = 6382 }
    ]
  }
}